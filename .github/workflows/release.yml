name: release-action
on: [release]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: .github/release.sh
        shell: bash
      - run: .github/createImage.sh
        shell: bash
